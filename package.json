{
  "name": "eslint-config",
  "devDependencies": {
    "eslint": "^8.3.0",
    "lerna": "^4.0.0",
    "prettier": "^2.4.1",
    "typescript": "^4.5.2"
  },
  "private": true,
  "scripts": {
    "lint": "prettier -c --ignore-path .gitignore .",
    "verup": "lerna version --no-push"
  },
  "workspaces": [
    "packages/*"
  ]
}
