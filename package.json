{
  "name": "eslint-config",
  "devDependencies": {
    "eslint": "^8.11.0",
    "lerna": "^4.0.0",
    "prettier": "^2.5.1",
    "typescript": "^4.6.2"
  },
  "private": true,
  "scripts": {
    "lint": "prettier -c --ignore-path .gitignore .",
    "verup": "lerna version --no-push"
  },
  "workspaces": [
    "packages/*"
  ]
}
