{
  "name": "eslint-config",
  "devDependencies": {
    "eslint": "^8.14.0",
    "lerna": "^4.0.0",
    "prettier": "^2.6.2",
    "typescript": "^4.6.4"
  },
  "private": true,
  "scripts": {
    "lint": "prettier -c --ignore-path .gitignore .",
    "verup": "lerna version --no-push"
  },
  "workspaces": [
    "packages/*"
  ]
}
