{
  "name": "eslint-config",
  "devDependencies": {
    "eslint": "^8.23.0",
    "lerna": "^5.4.3",
    "prettier": "^2.7.1",
    "typescript": "^4.8.2"
  },
  "private": true,
  "scripts": {
    "lint": "prettier -c --ignore-path .gitignore .",
    "verup": "lerna version --no-push"
  },
  "workspaces": [
    "packages/*"
  ]
}
